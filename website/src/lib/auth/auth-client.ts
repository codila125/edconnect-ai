// This file is used to create an authentication client using Better Auth.
// It initializes the client with default settings, which can be customized as needed.
// This file is from Better Auth and should not be manually edited.

import { createAuthClient } from "better-auth/react";

export const authClient = createAuthClient({});
